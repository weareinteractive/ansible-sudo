---

- name: Adding users
  template:
    src: "{{ item }}.j2"
    dest: "/{{ item }}"
    validate: "visudo -cf %s"
    owner: root
    group: root
    mode: "0644"
  with_items:
    - "etc/sudoers.d/ansible"
